{
  "name": "psp-complete",
  "version": "1.0.0",
  "main": "firebase-config.js",
  "scripts": {
    "build": "node generate-config.js",
    "start": "node generate-config.js && npx serve .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Problem Solution Platform MVP",
  "dependencies": {
    "firebase": "^12.10.0"
  }
}